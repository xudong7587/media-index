# MediaIndex 0.3.1

## 修复

- 电影候选的标题匹配只读取候选标题，不再让正文中的搜索词制造虚假命中。
- PanSou 日期合辑仍会被验证，但只有分享内部存在明确目标片名时才保留。
- 候选标题弱且文件名也弱的分享会直接丢弃，不再生成大量待确认卡片。
- 只有失效或空文件的候选不会再让电影任务误入待确认状态。

## 验证

- 使用《海洋奇缘：启航》真实 PanSou 返回复现并验证。
- 修复后结果为 `no_resource`、`requires_review=false`、`candidate_count=0`。
- 83 项自动化测试全部通过。
