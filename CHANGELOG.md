# MediaIndex 变更记录

## 0.7.14

- 互动渠道与网页链接转存不再把媒体文件直接散放到云下载分类目录：提交网盘前会先按用户确认的媒体名和年份创建独立暂存目录；未提供名称时使用稳定的链接标识隔离，115 与夸克复用同一目录规则。
- 修复新剧或 TMDB 季详情暂未建立时返回 HTTP 404 导致定点整理中断的问题：只有全部视频均能从文件名证明同季明确集号时才降级生成整理计划，并继续使用 TMDB 标准片名、年份、媒体目录和 `Season N` 目录落盘。
- TMDB 网络、鉴权和服务端错误仍按失败处理；季详情 404 但集号不完整时进入待复核。OpenList/115 补齐继续只在标准改名、建目录和最终对象核验完成后触发。
- 本版本不新增数据库迁移、配置项、环境变量或部署方式变更；现有云下载分类目录配置保持兼容。

## 0.7.13

- 网盘工作台 Webhook 升级为通用连接中心：可新建独立的接收型或发送型连接，统一展示方向、验证状态、最近事件和投递历史；MDC-NG 作为不可删除的内置适配器纳入总览，原 URL、密钥、目录授权、回执和增量 STRM 行为保持不变。
- 通用 Webhook 接收 CloudEvents 1.0 JSON，支持 Standard Webhooks HMAC-SHA256 或 Bearer 兼容鉴权、五分钟重放窗口、事件 ID 持久化去重和 256 KB 请求上限；通用接收只形成事件记录，不把第三方 JSON 当作转存、删除或配置命令执行。
- 通用 Webhook 发送可订阅转存成功、失败、待确认、入库、暂无资源和播放通知，使用 Standard Webhooks 签名头与持久化投递箱；目标真实返回 2xx 后才标记已连通，失败按指数退避恢复并支持手动重投。公网目标强制 HTTPS，局域网和 Docker 服务名保留 HTTP 兼容。
- 修复互动夸克链接/搜索转存被 OpenList 提前阻塞的问题：互动入口继续先走指定云下载直属子目录，整理器完成 TMDB 核对、标准改名、媒体/季度目录创建和最终对象逐项核验后，才以最多两个并发工作线程异步启动 115 补齐；整理未接管、失败或待复核时禁止从原始暂存目录调用 OpenList。
- 115 补齐保持“先核对 115 目标 → PanSou 搜索并验真原生 115 → OpenList 只复制精确差集”的顺序，补齐状态持久化在整理任务中并支持进程重启恢复，失败不回滚已完成的夸克整理。
- 全局设置新增默认关闭的开发者远程诊断：登录用户可创建 5–120 分钟、固定 `diagnostics:read` 权限的一次性显示令牌；服务端只存摘要，接口仅返回脱敏事件/单任务时间线，并具有限流、禁止缓存与一键撤销。

## 0.7.12

- 修复夸克分享链接转存 HTTP 400：按分享内容的真实父目录提交 `pdir_fid`，不同父目录分组、单批最多 50 个文件，并等待远程任务、完整分页清单、移动任务和最终目录核验完成。
- 修复夸克转存已提交后暂存文件无法唯一识别：每次尝试使用独立暂存目录，按来源文件名与大小稳定匹配；远端结果尚未稳定时保留暂存现场并明确失败，不再把未确认结果当作成功。
- 新增统一的夸克到 115 补齐流程：夸克确认落盘后先检查 115 已有文件，再通过 PanSou 搜索并验真 115 资源，优先原生 115 转存，OpenList 只复制仍缺失的精确文件；覆盖 TG、企微、网页直链、普通搜索转存、批量任务和愿望单，智能追更继续按季度显式启用。
- OpenList 自动补齐严格限制为夸克到 115，补齐失败与夸克主任务隔离；OpenList 页面补充启动条件、调用顺序、适用入口和批量/追更边界说明。
- 本版本不新增数据库迁移、API、配置项或环境变量；自动同步默认方向调整为夸克到 115，旧 `bidirectional` 值继续兼容读取但不再执行 115 到夸克的反向自动复制。

## 0.7.11

- 修复愿望单电影在夸克转存成功、115 暂无资源时的断链：OpenList 自动同步允许夸克到 115 时，继续精确复制、落盘确认、STRM/Emby 后处理与通知，完成后移除该媒体的愿望单条目。
- 发现详情在“夸克有可用资源、115 无资源”且 OpenList 自动补齐可用时，明确显示“转存到夸克并同步到 115”，并向既有转存批次传递补齐标志；OpenList 关闭或方向不允许时保持原行为。
- 本版本不新增数据库迁移、配置项、环境变量、路径或部署方式；现有客户端转存请求保持兼容。

## 0.7.10

- 重做手机端顶栏与密度：导航、标签、标题、正文和页面间距在窄屏更紧凑，表单字号仍保持 16px 以避免 iPhone 自动放大。
- TG 频道追踪改为 Bot 实时新帖与 PanSou 公开频道搜索的双通道；支持从 PanSou 勾选/全选导入、批量选择删除 MediaIndex 本地频道，导入和删除都不修改 PanSou 勾选。
- 转存规则将 115 放到夸克左侧，移除与 PanSou 重复的资源排除关键词；链路概览删除多余背景框。
- 使用手册顶部目标改为静态说明，01–09 目录只在页内定位；各流程子卡片整体直达对应页面，不再使用独立“调整设置”按钮。
- 本版本新增仅删除 MediaIndex 本地频道与索引的批量 API；不新增数据库迁移、环境变量、Docker 挂载或部署方式变更。

## 0.7.9

- “网盘工作台 → 资源获取”将 PanSou 与 TG 频道追踪拆为两个独立标签；PanSou 反向关键词继续只作用于聚合搜索，与所有 TG 频道规则隔离。
- TG 频道追踪按“运行设置 → 频道目录 → 当前频道独立规则 → 运行记录”重组；频道目录支持搜索、状态筛选和分页，旧版仅收集但尚未启用自动转存的来源集中标为“待配置”，不再形成超长混合列表。
- 每个 TG 频道独立维护启用状态、自动转存、正向/反向关键词、自动分类或指定云下载直属子目录；全局追踪开关与频道规则分开保存，现有转存、云下载整理、STRM、Emby 和通知流程不变。
- 左侧主导航新增与“全局设置”同级的“使用手册”；手册按首次配置、资源入口、云下载整理、直接入库、持续自动化、STRM/Emby、外部联动、排障和升级安全九章组织，提供完成标志与功能直达入口。
- 保留旧 `#settings-guide` 地址到新版一级手册页面的兼容映射；本版本不新增数据库迁移、配置项、环境变量或 API 变更。

## 0.7.8

- 完成 TG 频道自动追踪闭环：公开频道按可配置间隔轮询，私有频道复用 Bot 新消息；每个频道独立配置正向/反向关键词，并将全部通过规则的夸克、115、磁力或 ED2K 资源去重提交到云下载。
- TG 频道支持保守自动分类或指定真实云下载直属子目录；自动分类和目标映射不唯一时 fail closed。云下载整理完成后继续复用正式媒体库、定点 STRM、Emby 与通知后处理，不新增旁路。
- PanSou 反向关键词保存时不再因只编辑关键词而清空服务地址；TG 消息和候选预览统一脱敏资源链接。
- 运行日志支持逐条停止活动任务、逐条清除终态记录；批量清除覆盖待复核等所有终态，同时在服务端跨浏览器隐藏，排队、运行和等待重试任务保持可见。
- MDC-NG 用完成事件的目标路径在同一 Provider 的多个互动等待任务间消歧，精确匹配后才关闭任务；仍不唯一时保持等待。
- 全局设置中的使用手册按“入口 → 云下载暂存 → 安全整理 → 正式库 → STRM/Emby”完整流程重组，并把 TG 入口并入原有自动化概览。

## 0.7.7

- 资源获取新增与 TG 规则独立的 PanSou 反向关键词；TG 频道入口纳入资源获取页与云下载暂存整理链概览。
- 运行日志将“清除历史”和“停止运行”拆为独立操作；终态历史在服务端隐藏，活动任务继续保留。
- MDC-NG 完成事件只在互动渠道等待任务唯一时确认其终态，并继续关联 STRM 后处理；多候选时不猜测完成对象。
- 全局设置新增按完整媒体流程编排的使用手册，并同步补充 README 使用步骤和自动化安全边界。

## 0.7.6

- 企业微信回调新增五分钟时间窗校验和数据库持久化去重，阻止旧签名消息在进程重启或内存去重过期后再次触发写操作；现有回调地址、密钥和企业微信配置无需调整。
- 明文密钥读取响应强制禁止浏览器与代理缓存；Webhook 保持既有 URL Token 兼容，同时文档优先推荐请求头传递，避免新接入把密钥写入访问日志。
- 发布供应链固定 GitHub Actions 与 Docker 基础镜像摘要，CI 使用与正式镜像一致的 Python 锁定依赖；补充 STRM URL 属于长期播放凭证的安全说明。
- Docker Compose 端口绑定及现有 Emby、MDC-NG、企业微信配置保持不变。

## 0.7.5

- 修复手动与自动 STRM 生成被已删除资产的旧路径映射阻断的问题；只有 `removed` 状态的旧占用会被安全退役，活动资产的同路径冲突仍进入待复核。
- OpenList 从夸克复制到 115 后，原生 115 已配置时会等待精确文件落盘并取得文件 ID，再生成定点 STRM 和继续 Emby 后处理；未配置原生 115 的 OpenList-only 模式继续保留原有提交成功语义。
- 运行日志“清空日志”现在会同步调用 OpenList 原生清理接口，删除已完成、失败和取消的复制任务，不影响仍在运行的任务。

## 0.7.4

- 修复电影发现卡片被空集数计划误过滤的问题：只有夸克资源时单盘转存与“转存全部”也会正常提交；企业微信转存复用同一份已验真短期资源快照，避免 100% 可靠候选进入后续链路后再次漂移。
- 企业微信“服务器信息”新增 Emby 按钮，返回电影、剧集、单集数量和当前活跃/播放用户；愿望单反馈按媒体合并双网盘状态，并保留已完成、停用等历史状态，不再重复显示同一媒体。
- 浏览器扩展源码正式纳入主仓库并同步到 v0.7.4；恢复直链契约 v2、无副作用命名预览和直接入正式媒体库后的精确 STRM/Emby 后处理，CI 新增扩展契约回归测试。

## 0.7.3

- 修复 STRM 播放入口始终经 NAS 流式代理的问题：MediaIndex `/api/play` 现在按播放器实际 User-Agent 获取网盘临时直链并返回真实 302；仅在网盘要求 Cookie 等无法随跳转携带的请求头时明确回退代理。
- 补齐 Emby 反代 302：改写 MediaIndex STRM 的 PlaybackInfo，拦截外网客户端的 stream、universal 和 original 请求并直接返回网盘 CDN 地址。原生 Emby 端口仍保持普通 NAS 中转，302 只作用于 MediaIndex 播放反代端口。
- OpenList 手动跨盘复制新增 115 精确落盘确认和后处理节点；确认后仅对本次文件生成 STRM、立即请求 Emby 刷新，并等待 Emby 入库 Webhook 发送图文通知，不再把复制请求提交成功当作流程完成。
- 左侧主导航的 OpenList 标签更名为“跨盘转存”；内部 OpenList 配置项、接口和既有路径保持不变。

## 0.7.2

- 缩短 STRM 入库反馈链路：电影、单集和 MDC-NG Webhook 在 STRM 实际新增或替换后立即请求 Emby 刷新，不再经过固定 Webhook 等待；显式选择的 Emby 媒体库会跳过重复目录发现请求。微信图文仍严格由 Emby 入库 Webhook 回执触发，STRM 完成或 MDC-NG 回调本身不会提前宣告入库。
- 保留有依据的剧集聚合：只有 PanSou 冻结快照能够预见两集以上连续资源时，才对 Emby 返回的连续入库事件延迟合并通知；双网盘子任务继续由父批次统一收口，普通电影、单集和 Webhook 产生的 Emby 入库回调默认即时发送。
- 补齐全局设置各级标签栏的重点色悬停反馈，包括设置一级导航、通知分区、通知渠道、网盘设置和分段选项；Webhook 页面同步移除已经失效的合并等待输入项。

## 0.7.1

- 修复发现页搜索状态无法随一级标签退出的问题；多关键词搜索会分别调度 TMDB 标题和人物资料，并以人物演职员作品与标题结果的交集定位目标，例如“1942 冯小刚”可排除无关同名作品。
- 收紧发现推荐质量：口碑精选与最近上线必须具有海报和明确上映日期，口碑精选至少需要 200 个 TMDB 评分样本；补齐二级筛选标签的绿色悬停反馈。
- “待确认”迁移到“订阅与追更”的愿望单右侧；确认候选后立即刷新追更任务执行态并继续原生网盘转存。综艺匹配把 TMDB 播出日期、期数和上/下或（一）/（二）等价分段作为最高优先证据，减少可自动判断内容进入待确认。
- 智能追更卡片中的运行与存储诊断改为中性信息色，不再使用红色正文；删除等危险操作仍保留明确的危险色提示。
- Webhook 增量扫描目录改为地址框右侧点选，可在 STRM 页面已授权的一级范围内继续进入任意深层子目录；服务端保存和执行同时校验后代关系，仍拒绝越界或整盘扫描。
- TG 频道资源发现继续保持关闭：运行时不会搜索或合并 TG 频道候选，接口和数据结构仅作后续开发兼容保留。
- 修复“系统设置 → 网络代理”仅显示但未稳定作用于容器出站请求的问题：前端可直接填写 NAS 可达的局域网 HTTP/HTTPS 代理并从容器内测试 TMDB，网页保存值优先于 Compose 默认值；Compose 同时支持 `PROXY_URL`、`HTTP_PROXY`、`HTTPS_PROXY` 和 `NO_PROXY`，内部 Docker/局域网服务自动直连。
- 本版本不新增数据库迁移，不改变现有媒体路径、Docker 挂载、Emby 同步删除链路或升级方式。

## 0.7.0

- 发现预搜索改为并发准备 TMDB 身份与 PanSou 资源，并在同时探测夸克、115 时共享同一轮短时搜索快照；搜索词收敛为中文标准名、必要的季标记与电影年份，不再默认轮询英文名、繁体名、单集号或日期排列。
- 资源快照会把同一搜索中多个兼容链接完整交给转存批次，并在剧集卡片显示 TMDB 总集数、已播集数、当前可转和缺失数量。卡片直接转存冻结已验证计划、不再后台二次搜索；“转存全部”只提交当前有可靠候选的网盘，“加入智能追更”先执行同样的可用计划，再为所有已启用网盘保留后续检查。
- 单盘或全部转存启动后，媒体详情按已启用网盘展开独立执行链，未参与本批次的网盘明确显示“本次未启动”。流程把“提交网盘”和“落盘确认”拆开：原生夸克、115 与 QAS 只有核验目标目录及规范文件后才完成落盘，外部执行端仅提交成功时不会伪装为已落盘。
- 修复智能追更卡片的 OpenList 同步操作在窄控制栏和缺集面板中越界：顶栏使用紧凑 `⇄` 同步符号，缺集操作改为稳定两列，并约束图标与文字收缩范围。
- OpenList 地址、Token、两侧挂载路径、测试和自动补偿开关集中到顶层“OpenList 跨盘补齐”页面；全局设置移除重复入口，自动补偿只允许在夸克成功而 115 同一资产缺失时执行夸克到 115。
- 网盘工作台一级入口整理为“网盘链接、资源获取、转存和整理规则、云下载、Webhook、任务中心”；Webhook 不再藏在通知设置中，旧 OpenList/Webhook 路由继续兼容跳转。
- MDC-NG Webhook 新增固定扫描目录：从对应网盘已保存的 STRM 子目录中选择一项后，外部只需发送刮削完成信号，MediaIndex 会忽略请求路径并对该目录执行非删除增量扫描；未迁移旧配置继续兼容原安全路径映射。
- 提取无副作用的 `media-plan/v1` 与统一覆盖率读模型，发现短期快照、批量转存、企微/Telegram、浏览器插件 API 合同和云下载整理计划共用媒体身份、集数、覆盖率及首选链接；远端执行、恢复和清理仍保持独立。
- 新增与用户运行日志分离的后台诊断事件时间线和脱敏 ZIP 导出，记录任务状态迁移、工作流节点、写操作响应与异常；默认保留 30 天且最多 50000 条，不导出 Cookie、Token、分享链接或原始网盘响应。
- 暂停 TG 频道源作为资源发现候选：资源获取页面仅保留 PanSou，电影、剧集和追更解析不再查询或合并 TG 频道索引；现有 TG 接口、交互通知与数据结构保留，便于后续独立优化后恢复。
- 本版本新增可自动迁移的诊断事件表，不改变业务任务数据、Docker 挂载、媒体目录或既有单链接 API 调用。

## 0.6.21

- 从发现页或媒体详情加入智能追更时，会为已启用的夸克与 115 分别登记追更并立即检索、并行执行原生转存；两条网盘链路独立展示和结算，未启用对应 STRM 自动生成的网盘在转存完成后即结束，不再阻塞另一条链路。
- 优化剧集资源检索与补齐：PanSou 优先只搜索剧名和别名，不把单集编号拼入搜索词；115 同时支持整季资源包和逐集多链接，并可继续处理前 20 条之后的候选，按实际缺集逐集转存。
- 多集转存改为批次收口：全部已上线缺集和双网盘链路完成后，再集中执行一次 STRM 生成、Emby 刷新与通知；后处理失败只按精确结果重试后处理，不会重复提交网盘转存或逐集发送通知。
- 修复 115 目标目录不存在时无法追更的问题：目录选择器允许确认尚未创建的安全路径，首次转存时自动逐级创建；中断恢复会识别暂存区和目标目录中已完成的规范文件，避免重复接收。
- OpenList 自动补齐移至智能追更按季设置，仅在夸克已有而 115 缺失时执行夸克到 115 补齐；发现和媒体详情不再发起自动跨盘复制，手动补齐所选集不再要求同时启用两边追更，115 到夸克入口暂时禁用并明确提示不支持。
- 调整媒体详情和追更界面：双网盘使用独立资源卡与流程链路，转存按钮明确为同时转存两边网盘，移除“存本地”，并修正目录图标按钮尺寸及加载动画样式。
- 本版本为追更任务新增默认关闭的 `openlist_fallback_to_p115` 字段，并增加向后兼容的接口参数；已有追更任务、配置、媒体目录、Docker 挂载和部署方式不变。

## 0.6.20

- 统一互动渠道的资源入口：夸克分享进入夸克云下载，115 分享、磁力与电驴链接进入 115 云下载；先按编号选择云下载根目录的直属子目录，可追加准确媒体名称和年份作为识别提示，资源仍由后续云下载整理完成标准化命名与正式入库。
- 支持直接发送“资源名称 年份”进入与发现页一致的 TMDB 搜索、资源获取和目录选择流程；未找到资源会明确通知，连载内容按 TMDB 播出日期自动加入智能追更，默认检查时间调整为 12:00。
- 智能追更页面新增失败重试间隔与最大重试次数设置；新任务和历史空时间使用 12:00 默认值，已有有效任务时间保持不变。
- 修复 Webhook 引入媒体后的单文件入库链路：兼容 `target_path`、`file_path` 与 `path`，按已完成文件定点生成 STRM，并仅在实际变更时通知 Emby 刷新，不再回退整库扫描；外部整理器仍负责命名。
- 全局设置新增首位“链路概览”，统一呈现发现、云下载、智能追更、愿望单、互动链接与 Webhook 引入媒体到核验、整理转存、STRM 和 Emby 入库的状态，各节点可直接跳转对应设置。
- 重绘当前可达页面的视觉系统：采用中性底色和分层磨砂卡片，统一表单、按钮、状态、弹窗、字号与响应式规格；链路汇流使用自适应 SVG，固定节点、箭头与图标不再被拉伸，并兼容暗色、减少动态和减少透明效果偏好。
- 本版本改变互动渠道的默认转存落点，并调整数据库与配置的追更时间默认值；新增字段和 Webhook 参数均向后兼容，不覆盖已有有效配置，不改变 Docker 挂载、既有媒体目录或部署方式。

## 0.6.19

- 云下载根目录改为可选网盘内任意安全绝对路径，不再强制位于该网盘的正式媒体库根目录下；仍在实际来源分类与目标分类层面拒绝同路径或相互包含的危险映射。
- 整理触发方式支持“前序动作事件”与“定时检查”单选或多选；事件模式继续按本次转存的精确文件定点处理，定时模式恢复稳定等待、单实例合并执行与已授权范围检查，仍不提供手动全量整理入口。
- 云下载页面改为 115 在上、夸克在下的纵向布局，未开启的网盘折叠详细设置；整理范围默认授权云下载根目录的全部一级子目录，只有选择“部分目录”时才读取并勾选子目录。
- 新配置字段保持对 0.6.18 已有事件驱动配置和旧子目录勾选范围的升级兼容；不新增数据库迁移，不改变 Docker 挂载、已有媒体路径或部署方式。

## 0.6.18

- 云下载整理改为由原生夸克、115 转存完成事件直接触发：只核验本次任务返回的精确文件 ID、名称与目标路径，随后执行 TMDB 核对、标准目录与命名、复制或移动、定点 STRM、Emby 入库及通知；移除周期性全量/增量扫描入口，拒绝在缺少精确目标时回退扫描。
- 夸克与 115 增加独立整理开关；云下载根目录、正式媒体根目录与授权一级子目录统一迁入网盘工作台一级“云下载”页面，并按同名子目录一一映射。页面位于“转存和整理规则”与“任务中心”之间，补充完整流程、范围与安全说明。
- MDC-NG Webhook 改为按单个已刮削文件定点生成 STRM，只合并同一文件的重复完成事件，不再触发整库增量扫描；Provider 由服务端保存配置决定，外部请求不能覆盖。
- 夸克分享链接、交互网盘选择与 PanSou 候选身份统一使用原生 `quark` Provider；新任务通过夸克 Cookie 完成验真、暂存、改名、移动和结果确认，不再依赖 QAS 服务。历史 QAS 配置与任务数据仅保留升级读取兼容。
- 网盘、OpenList、STRM 与本地容器路径输入补充可用的目录选择按钮；UI 设计约束切换为 `emilkowalski/skills`。本版本不新增数据库迁移，不改变 Docker 挂载、既有文件路径或升级方式。

## 0.6.17

- 运行日志改为点击整张任务卡直接进入对应功能页面，移除卡片底部的额外跳转按钮；OpenList 复制队列同样可直接点击进入跨盘转存。
- 运行日志不再向用户展示内部任务编号；新增 CSV 导出，可按需导出最多 50000 条后台日志记录，导出文件也不包含任务编号。
- 新增“清空日志”，仅在当前浏览器隐藏清空前的历史记录，后台任务与编号不会删除；进行中、等待重试和待确认任务始终保留，新完成记录继续正常显示。
- 新增只读日志导出接口，不改变原任务列表接口、数据库结构、配置、文件路径、Docker 部署和升级方式。

## 0.6.16

- 发现页恢复“待确认”入口并放在“链接下载”旁边，旧 `#review` 链接也会直达该页面；继续复用原有候选筛选、文件勾选、确认、重新搜索与删除能力。
- 运行日志中的转存、待确认、OpenList、STRM、删除同步、追更、愿望单、云下载整理和 Emby 任务均可直接跳转到对应功能页面。
- 发现页媒体详情的自动入库流程改为紧凑阶段条，仅展开当前步骤的具体信息；OpenList 步骤在等待或跳过时隐藏，只在夸克已转存、115 缺失并实际进入跨盘补齐后显示。
- 本次仅调整前端导航与展示，不改变配置、数据库、API、文件路径、Docker 部署和升级方式。

## 0.6.15

- 网盘工作台新增“云下载整理”：可分别勾选 115 与夸克云下载根下的一级分类，并按同名分类映射到正式媒体库；支持独立总开关、复制/移动模式、自动检查间隔与稳定等待时间。
- 整理前通过 TMDB 保守核对媒体身份，按现有规则建立标准电影、剧集和 `Season` 目录并规范媒体、字幕与 NFO 名称；歧义、缺集、冲突或 Provider 异常统一进入可见失败/待核对状态，不猜测写入。
- 复制模式保留来源并支持幂等续作；移动模式仅在目标名称、大小与文件 ID 逐项核验后按精确源文件 ID 清理残留，永不整目录永久删除。完成后复用现有 STRM、Emby 刷新和通知链路；新配置默认关闭，无数据库迁移及 Docker、挂载或升级路径变化。

## 0.6.14

- 官方 GHCR 镜像改为同一标签同时发布 `linux/amd64` 与 `linux/arm64`，Docker 与 Compose 会按宿主架构自动选择镜像，无需新增 ARM 专用标签或修改现有部署文件。
- ARM 支持限定为 64 位 ARMv8；保留现有镜像名称、端口、挂载、环境变量、用户权限、数据库、配置和升级路径，不支持 32 位 `arm/v7`。

## 0.6.13

- 显著降低日常运行资源占用：媒体库封面能力只在打开预览、立即替换或定时更新时使用，任务结束后及时释放；同时限制同一时间的封面任务和短期素材数量，更适合资源有限的 NAS 长期运行。
- 保持封面工坊原有四种风格、实时预览、单库/批量替换、字体、分辨率和定时计划兼容；优化生成过程中的临时资源使用，不改变已有配置、媒体库封面操作和 Emby 接口。
- 从用户目标与使用流程重新编写 README，并把部署、发现、双网盘、整理、追更、愿望单、OpenList、STRM/302、Emby、封面、通知交互、Webhook、升级备份与排障整理为完整使用手册。

## 0.6.12

- 企业微信底部菜单和 Telegram 命令统一为 STRM、订阅管理、服务器信息三组；新增按编号选择 STRM 来源根目录一级子目录的定向扫描，并按逻辑媒体任务聚合追更与状态结果，减少多网盘重复项。
- 媒体库封面工坊恢复 MoviePilot MediaCoverGenerator 同源的四种大风格预览并校正 `static_1`～`static_4` 顺序；定时更新与更新计划等宽展示，底部新增“保存配置”，统一保存风格、标题、生成参数、媒体库范围和定时计划。
- 单库、批量和定时封面生成接入运行日志，展示生成中、完成、部分失败与异常；不可中断的封面和计划任务不会被“终止任务”或“全部停止”误改状态。

## 0.6.11

- 以 0.6.10 稳定运行代码为基准，正式建立 Discover、Tracking、Transfer、STRM、Media Server、Cloud、OpenList、Integrations、Settings 与 Activity 的业务归属、Shared/Core 规则和小 PR 工作流；本版本不改变功能、API、配置、数据库、路径、STRM 或部署行为。
- 完善 `AGENTS.md`、架构文档、开发流程和 PR 模板：每个任务声明一个 Primary Module，跨模块与 Shared/Core 修改必须记录兼容性、影响面和回归证据。
- 加强架构门禁：保持 Domain 纯净，冻结现有 Service→API、Provider/Client→Service 逆向依赖和前端跨 feature 私有引用，并识别 Python 相对导入及前端 side-effect/dynamic import，防止 legacy 耦合继续扩散。

## 0.6.10

- 加速 Emby 封面实时预览：复用短期海报与取样结果，单海报模板不再重复写入九张临时图，多海报模板按可用 CPU 并行准备素材。
- 封面字体、字号、位置、间距、输出规格和风格改为全部媒体库共用，仅中英文标题内容按媒体库保存；保存后下一次定时刷新会把统一样式应用到全部勾选媒体库。底部操作精简为立即替换预览媒体库和立即替换全部媒体库。
- 封面定时刷新改为带说明和可读描述的 5 位 Cron 表达式，服务端严格校验后按配置时区调度。
- 愿望单与智能追更页面统一增加自动巡检总开关；修复愿望单暂无资源分支触发 `UnboundLocalError`，巡检未命中统一显示“暂未找到资源”。计划任务日志不再误发外部失败通知，同一媒体的一次多集、多网盘或批次操作聚合为一条通知。

## 0.6.9

- 重构 Emby 媒体库封面工坊为常驻实时预览工作台：可直接切换预览媒体库和输出规格，四种静态风格始终可见，标题与生成参数停止调整 0.3 秒后自动刷新预览，只有明确点击生成按钮才写入 Emby。
- 每个媒体库可独立保存中文标题、英文标题、中文字体和英文字体；中英文字体分别提供系统字体选择与 TTF、OTF、TTC 上传入口，上传文件保存在持久化数据目录并以内容哈希安全引用。
- 中文大小、英文大小、水平位置、垂直位置、中英文间距和英文行距支持滑块与数值精调；修复静态风格 1/2 忽略英文行距以及风格 3 的装饰色条不跟随文字移动的问题，预览、单库替换、批量生成与定时刷新共用同一渲染配置。

## 0.6.8

- 同一 Emby 媒体文件夹的逐文件删除事件合并为一条运行日志和一张海报图文通知，同时保留每个 Webhook 事件的独立幂等键与唯一 115 文件 ID 校验；删除通知优先读取内网 Emby 图片，并在条目已消失时回退同目录海报或历史缓存。
- 修复媒体库封面工坊：增加基础设置、持久化媒体库范围和每库标题，按 Emby 图片接口要求上传 Base64 JPEG 到实际媒体库 ItemId，并在替换后强制刷新浏览器缓存。
- 企业微信和 Telegram 快捷菜单保存后立即同步，服务重启后自动恢复企业微信底部菜单与 Telegram 命令。

## 0.6.7

- 修复 0.6.6 STRM 对账跨来源根目录清理：完整限定网盘、来源根目录、媒体库标识与所选范围；增量扫描永不清理，完整全量扫描连续两次缺失才删除，并增加空清单、无视频清单、异常批量删除与批量路径迁移熔断。筛选规则变化不再被视为源端删除；所有增量入口取消 10,000 文件总量上限。
- 通知与交互新增通用增量同步 Webhook 子页：带密钥接收外部刮削器或整理器的完成事件，合并同批次通知后执行只增不删的 STRM 增量同步，并提供容器地址、连接测试和可复制的 `curl` 命令；扫描范围强制锁定对应网盘 STRM 页面已保存的根目录与勾选子目录，空选择会在保存、调度和执行三层拒绝，保留旧 MDC-NG URL 兼容。
- 媒体外部通知升级为海报图文优先；Emby 播放通知补充媒体、季集、用户、设备、地址、进度与简介等易读字段，图文失败仍安全回退文字。
- 修复 Emby 到 115 删除同步：公共 STRM 根目录优先于 Emby 单个媒体库目录还原相对路径，不再丢失 `02系列电影` 等首层目录；完整路径兼容大小写、URL 编码与 Unicode 规范化差异，仍只允许唯一 115 文件 ID；同一删除事件只处理一次。
- 夸克和 115 各自新增云下载目录。交互渠道与发现页直接链接按类型自动路由：夸克分享进入夸克，115 分享、磁力、电驴和普通下载链接进入 115。
- 发现页恢复独立“链接下载”入口；影视详情新增无需等待资源的“加入愿望单”，OpenList 补齐改为每次转存显式标记后才执行。
- STRM 通用设置隐藏路径自动匹配和后备媒体库选项，不改变现有 STRM 生成与媒体库匹配逻辑。
- 修复侧栏收起后品牌图标消失；悬停图标可直接展开工作区。手机端整体字号、间距和控件高度下调一档。
- 智能追更的时间保存按钮改为窄图标，修复与刷新按钮重叠；同步按钮使用双向箭头，窄屏自动隐藏文字并保留悬停说明。
- 智能追更的保存路径操作收进地址栏右侧的纯文件夹图标，并为原生夸克追更开放同样的目录选择与路径更新能力。
- 运行日志新增“计划任务”分栏，集中显示智能追更巡检、愿望单巡检和 115 生活监控；每类循环任务只保留最近一条状态。
- 115 与夸克 STRM 必须明确勾选并保存直接子目录，两端长期展示已选范围；未选时手动、Cron、转存联动和快捷指令均不扫描整盘。
- 愿望单新增单项巡检开关，继续使用 TMDB 上映/首播日期与用户自定义时间；网盘规则新增可自定义的排除关键词，在候选排序与分集文件选择两层排除 TC、抢先、预览版、480p 等低质量标记。
- 企业微信与 Telegram 新增可自定义快捷菜单：可对已开启且已选目录的网盘触发 STRM 全量/增量扫描，查看智能追更并跳转页面，或进入资源名/下载链接输入。
- 媒体库封面工坊接入 GPL-3.0 静态渲染器的四种风格与标题、来源排序、海报/背景图、分辨率、字号缩放、偏移、间距、模糊/渐变、主题色与定时刷新参数，UI 统一为 MediaIndex 浅绿色系；不包含动态封面。

## 0.6.6

- 115 STRM 新增可指定子目录的生活事件监控：首次启动只建立基线，检测到新变化后使用现有分页流程执行范围内的增量 STRM 更新。
- Emby 到 115 的删除同步现在会把事件接收、精确文件 ID 匹配、回收站执行、完成或失败写入右上角运行日志，并补充站内通知。
- 交互链接恢复从对应网盘保存根目录读取真实子目录并返回编号确认；115 与夸克目录来源分开说明，并可直达 115 保存目录设置。

## 0.6.5

- 发现页搜索框聚焦时只保留组件最外层的强调描边，移除输入框自身的内层轮廓与圆角边线。
- 恢复交互会话的下载链接自动处理：发送夸克、115、磁力、电驴或 HTTP 链接后直接进入目录确认与转存/离线下载流程，不再被历史开关拦截。

## 0.6.4

- 全局设置新增成人内容筛选，默认屏蔽 TMDB 标记的成人条目；发现页搜索焦点框与侧栏收起后的悬停展开交互同步修正。
- 修正 Emby 媒体库封面上传格式：预览确认后的 JPEG 现在按 Emby 图片接口要求直接写入，单库应用、批量应用和定时刷新共用同一条可靠写入链路。
- STRM 增量扫描取消 10,000 文件上限，改为与全量扫描相同的分页/批量只读遍历；增量模式仍不会清理未出现的历史映射，以降低自动任务风险。
- 通知设置新增可勾选的事件范围，包括转存完成、Emby 入库、待确认、暂无资源、处理失败和播放事件；未选事件只保留站内记录，不再外发。
- 图文通知继续优先使用已缓存的媒体海报，通过企业微信图文消息和 Telegram 图片消息发送，图片发送失败时安全回退文字。
- 删除联动回归覆盖了 Emby 标准 JSON、multipart、容器路径映射、目录删除、唯一 STRM 映射与 115 回收站执行；自动执行仍须由用户明确开启。

## 0.6.3

- 原生 115 运行时统一改为只使用 Cookie：目录读取、资源验真、转存、STRM 清单和离线下载均不再使用 Open 授权或 OpenList 作为认证回退。
- 网盘连接页仅展示 115 Cookie 的填写、保存和验证；旧 Open 代码、配置字段和历史凭据保留以兼容已升级实例，但不会再被视为已连接或参与执行。
- 115 高速 STRM 清单继续使用 Cookie 的只读批量接口，不会调用会在网盘生成“目录树.txt”的导出任务。

## 0.6.2

- “全量扫描更新”改为真正无 10,000 文件上限的手动全量遍历；不会再把已达到上限的部分结果标记为完成。定时/自动增量扫描仍保留单次 10,000 文件安全上限。
- STRM 运行日志在扫描期间显示当前正在读取的网盘目录路径（首个目录及每 25 个目录刷新一次），不会逐文件刷屏。
- 115 与夸克 STRM 来源目录可读取并勾选直接子目录；勾选后仅扫描这些目录，未选目录不会读取或生成 STRM。

## 0.6.1

- 媒体库封面工坊改用并保留归属的 MediaCoverGenerator 静态渲染器：圆角单海报叠层、九图斜向组合、单海报斜向构图与纯文字四种模板均使用 Emby 现有海报生成；不包含动画样式。
- 静态封面渲染期间仅在临时目录处理 Emby 海报，完成即清理；不在镜像中打包电影海报或上游预览图。
- 由于整合 GPL-3.0 的静态渲染器，MediaIndex 从本版本起按 GPL-3.0 发布；原作者、上游来源与移植范围见 `THIRD_PARTY_NOTICES.md`。

## 0.6.0

- 完成 STRM 与 302 播放闭环：独立播放端口、可配置内外网播放地址、Emby 自动刷新与 Webhook 删除同步均已可用。
- 115 STRM 的来源、输出、增量扫描、删除同步与网盘转存规则相互隔离；全量扫描保持只读目录枚举，避免调用会在 115 根目录生成目录树 TXT 的导出接口。
- 媒体服务器新增静态媒体库封面工坊：圆角海报堆叠、斜向多海报、单海报斜置与纯文字四种模板，支持预览、单库应用和定时刷新。
- 优化工作区与设置页布局：可收起侧栏、版本信息移出用户卡片、高级设置与规则区间距统一。
- 发现页缓存资源链接在执行前重新验证，失效的 115 链接不会被直接提交；仅当夸克已可转存、115 无可用资源且方向允许时，才通过既有 OpenList 复制链路补齐到 115。
- Compose、README 与使用手册更新 302 播放端口和 `/strm` 挂载的 0.6.0 升级要求；不再需要独立播放容器。

## 0.6.0-rc.21

- STRM 输出目录选择器允许选择挂载根目录下尚未创建的子目录，首次生成时自动安全创建。
- STRM 原子写入改用每次任务独立的临时文件，修复手动扫描、自动生成与 Cron 重叠时偶发的 `FileNotFoundError`。
- STRM 任务中心现在显示失败的准确阶段与安全路径，不再只显示笼统的 `FileNotFoundError`。
- 115 全量扫描继续固定使用逐目录只读元数据接口，不调用会在 115 生成目录树 TXT 的导出接口。

## 0.6.0-rc.20

- 115 与夸克 STRM 来源目录从网盘转存保存规则中拆分，自动生成、手动扫描与定时扫描均使用各自独立来源根目录。
- 115 与夸克 STRM 页面都支持网盘目录浏览；本地 STRM 输出目录支持在容器挂载目录内浏览选择。
- 新增每个网盘独立的 5 段 Cron 定时增量扫描；夸克删除同步保留独立入口并明确标记暂未支持。
- 115 删除同步归入 115 STRM 页面；全量扫描在移除本地 STRM 前先停用旧映射，避免 Emby 将路径矫正误判为源文件删除。
- 入库与删除通知按媒体文件夹聚合，延迟等待 Emby 入库后再推送，并继续优先使用已有海报发送图文消息。
- 左侧栏底部恢复用户信息与 GitHub、主题、退出按钮同排，版本号单独放在最底部。

## 0.6.0-rc.19

- 删除同步补充可保存的“Emby 中的 STRM 媒体库根目录”，用于 MoviePilot / 神医助手 Pro 与 MediaIndex 容器路径不同的场景。
- 左侧工作区新增可记忆的收起/展开按钮；收起后仅保留导航图标。
- 版本号移至左侧栏底部，并从全局设置保存栏移除。
- 修复通用分类路径保存后刷新仍显示旧值，并让各网盘在没有专属覆盖时继承通用分类。
- STRM 扫描与生成按当前选择的网盘来源根目录隔离，修复重复根目录及历史扫描文件混入当前输出的问题。

## 0.6.0-rc.18

- Emby 删除同步新增“Emby 中的 STRM 媒体库根目录”，支持 MediaIndex 与 Emby 容器路径不同及 Windows Emby 路径。
- 兼容神医助手 Pro 常见的 `ItemRemoved` 与 `ItemPath` / `FilePath` 删除负载；路径无法映射时明确返回配置错误，不再静默跳过。
- 删除同步页面明确提示必须开启“自动执行”才会按唯一 115 `file_id` 将源文件移入回收站。

## 0.6.0-rc.17

- Emby 删除 Webhook 保留 STRM 输出根目录之下的完整相对路径，同名 STRM 不再按文件名误匹配。
- 删除前要求该完整 STRM 路径唯一映射到一个 115 `file_id`；跨库根冲突、非 115 或不确定映射均拒绝执行。

## 0.6.0-rc.16

- Emby Webhook 同时兼容 `multipart/form-data`、`application/json` 和表单编码；优先解析 MP/Emby 常见的 `data`、`payload`、`json` 事件字段。
- 设置页面将 Emby Webhook 内容类型推荐值调整为 `multipart/form-data`，避免请求在进入通知中继前被 JSON 校验拒绝。

## 0.6.0-rc.15

- Emby Webhook 测试通知会复用已配置的企微/TG 通道实际发送测试消息，不再只返回 HTTP 成功。
- 真实 Emby 删除事件会在执行原有精确 STRM 删除同步的同时，通过全局已启用通知通道发送事件消息。

## 0.6.0-rc.14

- 已保存的 Emby Webhook 密钥通过眼睛显示后会同步生成完整 URL，无需重新生成或重复保存密钥。

## 0.6.0-rc.13

- 普通 URL、主机与路径配置在已登录的设置页明文显示；含凭据的 URL 仍会遮蔽密码部分。
- 已保存的 Cookie、Token、API Key 和 Secret 默认隐藏，点击输入框右侧眼睛后才按需读取并显示。

## 0.6.0-rc.12

- 兼容 Emby Webhook 的测试通知：正确令牌可直接验证成功，真实删除事件仍严格要求精确 `.strm` 路径。

## 0.6.0-rc.11

- Emby 删除 Webhook 支持把认证令牌放入完整 URL，页面可生成、显示和复制，兼容 Emby 无法附加自定义请求头的配置界面。
- 通知与交互增加 Emby 入库刷新和删除事件状态卡片，并可直接跳转到 STRM 通用设置或删除同步。
- 敏感配置输入框增加显示/隐藏按钮；普通地址和其他非敏感配置不再因字段名包含 URL/Host 而被遮蔽。

## 0.6.0-rc.10

- 明确区分容器内部 8097 与 Compose 宿主机播放端口：管理页面只读显示容器监听端口，不再误显示或猜测宿主机端口。
- Compose 只保留端口映射，无需额外设置 `EMBY_PROXY_PORT`；STRM 实际地址使用“STRM 通用设置”中填写的完整播放入口。

## 0.6.0-rc.9

- 修正单容器播放端口发布方式：Compose 固定映射宿主机 302 端口到容器内 8097，并将该端口锁定显示在管理页面。
- 保持管理服务与播放网关共用一个 MediaIndex 容器、配置目录、数据库和签名密钥，不再使用独立播放容器。

## 0.6.0-rc.8

- 将 STRM/302 播放网关合并进同一个 MediaIndex 容器，管理与播放使用两个端口，不再部署独立播放容器。
- 调整网盘工作台、PanSou/TG、转存规则和跨盘转存布局，加入紧凑可拖动质量顺序、任务折叠与进行中/已完成分组。
- 增加独立 Emby 媒体库封面工坊，提供四种静态样式、单库/全部生成和默认关闭的定时刷新。
- 兼容 Emby 标准 JSON 删除事件，页面直接给出 Webhook 地址、内容类型和认证请求头。
- 115 与夸克普通连接统一使用 Cookie，并改善目录缺失时的可读错误和历史追更进度保留。

## 0.5.15

- 智能追更会在调度和卡片展开时刷新 TMDB 分集元数据，新播出的集数可及时进入待追更列表；资源尚未更新时静默重试，不再反复生成无效的待确认通知。
- 改进综艺多日、多段文件的校验逻辑，可结合播出日期、期数和分段识别缺失内容；PanSou 搜索查询和候选排序逻辑保持不变。
- 设置页保存入口改为右下角固定圆形按钮；运行状态改为可关闭的居中日志弹窗，展示任务阶段、消息、路径和时间。
- 智能追更分集下拉列表使用简洁的 `E26`、`E27` 标签，并避免 115 目录读取阻塞 TMDB 分集显示。

## 0.5.14

- 发现卡片转存会保留预搜索选中的分享链接并直接校验、转存，不再重复执行 PanSou 搜索流程。
- 修复批量转存请求丢失指定 115 分享链接的问题；所选链接验证失败时展示真实的 115 连接错误，不再误报“PanSou 没有找到资源”。
- 修复 115 与 MoviePilot 已提交任务长期显示为进行中，以及单向 OpenList 同步错误提示反向复制的问题。
- 115 转存支持从完整暂存文件断点续跑，并在暂存成功后再创建最终目录；电影失败时保留真实网盘错误。

## 0.5.13

- 修复 PanSou 结果按全局数量截断导致 115 候选被夸克结果挤掉的问题，按网盘类型公平保留结果并等待异步插件返回。
- 115 Cookie 或网络暂时不可用时保留已搜索到的候选，明确提示验证原因，不再误报没有资源；兼容遗留 115 Open 配置与有效 Cookie 并存。
- 完善电影、电视剧和智能追更的候选验证状态与进度反馈。

## 0.5.12

- 增加 OpenList 自动同步方向设置，支持夸克到 115、115 到夸克和双向同步；手动同步与智能追更手动补齐不受方向限制。
- 修复 115 已有资源、夸克无资源时误建愿望单的问题，并收敛自动同步、P115 环境隔离、Telegram 线程和配置回滚边界。
- 磁力、电驴及普通下载链接统一使用 115 离线下载，完善相关设置与使用文档。

## 0.5.11

- 优化 PanSou 优先的电影与标准媒体处理流程，减少不必要的 TMDB 匹配并改善资源转存反馈。
- 统一企业微信与 Telegram 的通知、交互指令和转存记录设置，补充 Telegram 交互与自动同步状态反馈。
- 统一设置页视觉语言，收窄桌面布局并完善手机端响应式显示。

## 0.5.10

- 修复智能追更完成后按媒体目录执行 OpenList 定向同步，避免误触发全库递归同步。
- 补充追更路径、标题别名和已存集数处理，减少跨网盘同步找不到源文件的问题。
- 暂时停用从 115 复制到夸克的 OpenList 操作入口，并在设置和追更卡片中明确提示。
- 整理 115 Cookie/115 Open 授权、配置导入导出和巡检设置相关说明。

## 0.5.5

- 设置改为基础设置、网盘设置、OpenList 同步、通知和交互、愿望单与网络代理等独立页面；网盘目录改为点选，并提供连接状态与任务状态反馈。
- 115 支持从 OpenList 导入 Cookie 或 115 Open access/refresh token；Open 凭据可读取个人目录并提交 magnet、ed2k、HTTP/HTTPS 离线下载。
- 115 Open 直连不可用时，目录选择和离线下载会按 OpenList 中该存储的实际挂载点回退；115 分享链接转存仍要求 Cookie。
- 配置导入导出包含全部业务设置、网盘凭据与目录、智能追更任务和已保存集数状态、愿望单任务；导入不会覆盖目标实例的登录与运行时配置。
- 智能追更目录扫描遇到不可识别文件或空结果时保留已有进度，避免历史集数被错误重放。

## 0.5.3

- 修复新建智能追更任务的下次巡检时间：当 TMDB 已提供未来下一集发布时间时，自动追更会排到该发布时间，不再因历史未保存集立刻执行。
- 自动追更执行时会尊重建档起点，避免目录为空的新任务自动回头处理历史集；历史缺集仍可由用户手动补集。
- 修改追更时间、启用第二网盘和恢复追更时，都会按 TMDB 播出日期重新计算下一次巡检时间。
- 记录 Windows 到 NAS 的稳定源码包上传流程，避免 `scp` 或二进制管道不稳定时反复试错。

## 0.5.2

- 修复 115 SDK 在容器内默认写入 `/root/.p115client.cache.d` 导致离线下载提交失败的问题；现在会把 SDK 缓存固定到 `/app/data/cache/p115client`。
- 强化 115 连接测试：除目录读取外，也会用只读探测确认 115 离线下载接口可用，并兼容部分 SDK 接口返回 `undefined action` 的情况。
- 优化企业微信交互发送磁力、ed2k、HTTP/HTTPS 下载链接到 115 的反馈：提交后会短轮询 115 任务列表，若 115 已秒存完成会返回“云下载完成”，失败会返回 115 的具体原因，仍在处理时才提示已提交/处理中。
- 保留 115 SDK 返回的可读错误信息，减少只显示 `PermissionError` 或通用权限错误导致无法判断原因的问题。

## 0.5.1

- 强化原生 115 Provider：支持分享读取、文件匹配、转存、重命名、移动和目标目录确认，并支持通过 115 离线下载提交磁力、ed2k、HTTP/HTTPS 链接。
- 优化发现页和详情卡片：支持刷新资源、单网盘转存、转存全部有资源网盘，以及直接加入智能追更。
- 完善智能追更：刷新时读取网盘真实目录状态，只从最后已存集的下一集开始追；历史缺集需用户手动补齐。
- 增加多网盘 OpenList 自动同步：双网盘转存或追更新集后只同步缺失文件，相同目录同步任务运行中不重复提交。
- 增加 OpenList 手动同步页面排序功能，支持按文件名、类型和时间正序/倒序切换，目录点击进入且保留勾选复制。
- 增加统一命名规则、按季目录、分类路径原生目录选择和目标文件夹宽松匹配。
- 增加右上角执行任务面板、全部停止按钮和运行中任务动态状态。
- 通知模块新增企业微信交互下载链接自动转存：夸克/115 分享链接直接转存，关联网盘为 115 时支持离线下载链接。
- README 和使用手册重写，突出 115、智能追更、自动同步和通知离线下载等功能。

## 0.5.0

- 原生 115 支持 `115cdn.com` 分享链接和查询参数中的提取码，可直接验链与转存。
- 发现页按网盘展示紧凑资源按钮，支持单网盘转存、复制分享链接和一键转存全部有资源网盘。
- 智能追更与愿望单支持切换目标网盘；愿望单增加“立即执行”。
- 智能追更可展开季度集数，按已存状态选择缺失集并手动补齐。
- 设置按夸克与 115 分离保存根目录和分类目录，并支持自定义分类。
- 原生 115 增加独立本地下载目录；网盘暂存目录继续仅用于安全接收、改名和移动。
- 手动补集提示 PanSou 对较早资源的召回限制，愿望单操作区固定网盘按钮宽度并优化日期顺序。

## 0.4.17

- 新增原生 115 Provider：由 MediaIndex 验证分享、匹配文件、转存、改名、移动并核对目标目录，不依赖 MoviePilot 执行转存。
- 设置页支持直接粘贴 115 Cookie，或填写 MoviePilot 地址和 API Key 后从 `P115StrmHelper` 安全导入；配置接口不返回 Cookie。
- 发现页按 `Provider × 季` 并行验证 QAS 与 115，支持单季、最后一季和手动多季批量转存。
- 新增父批次与独立子任务状态；一个网盘失败不会回滚另一个网盘的成功结果，部分成功会单独通知失败原因。
- 数据库补充 Provider、外部任务和批次兼容迁移，旧 QAS 数据继续按原行为运行。
- Bark、Emby 播放联动及 115 扫码/OAuth 已纳入后续路线图，本版本不开放这些未完成能力。

## 0.4.16

- 追更搜索不再使用“喜单”“脱友”等过短别名，优先使用完整标题、发布日期和季信息，减少 PanSou 噪声召回。
- 剧集和综艺候选标题与目标明显无关时直接拒绝，不能再凭短剧目录里的 `12.mp4/13.mp4` 获得集数覆盖分。
- 支持同一期后半段文件，例如“第3期（三）/（四）”按日期连续映射到 TMDB E12/E13。
- 同批目标共享的日期或“第3期”等令牌不再单独用于定集，并且只把真正匹配到文件的集数标记为已转存或已触发。
- 根目录 Compose 增加默认注释的 PanSou 和 QAS 完整部署段，并预填同网络容器地址。
- 新增完整项目使用手册，覆盖首次配置、追更、待确认、更新与备份。

## 0.4.15

- PanSou 候选只包含网盘已经保存的旧集时，反馈“当前已追到可用最新集，无需转存”，不再生成大量无效待确认资源。
- 资源源尚未更新属于正常等待状态，不会因多次重试升级为人工确认；真正存在目标集但文件选择有歧义时仍保留确认流程。
- 资源源尚未更新时固定一小时后复查，手动刷新次数不会把等待时间拉长到四至十二小时。

## 0.4.14

- 支持通过通知设置中的公网地址，让企业微信和 Telegram 读取 MediaIndex 后端缓存海报。
- 手动追更检查结果等直接通知也会立即投递外部渠道，不再只显示在网页通知中心。
- 企业微信自建应用、群机器人和 Telegram 继续优先发送图文消息，失败时回退为文字。

## 0.4.13

- 手动追更会重新搜索已取消的待确认集数，并强制刷新 PanSou 搜索缓存。
- 综艺同一天更新多集时优先加入 `标题 + MMDD` 日期关键词，提高当天新资源命中率。
- 支持把 `同一日期 + 第N期(一)/(二)` 等连续分段安全映射到 TMDB 连续集序号。
- 删除全部待确认候选后，关联追更或愿望单恢复为可重试状态，不再残留为 `needs_review`。
- 重新搜索同一批集数时自动归档旧失败任务并释放幂等键，同时继续阻止运行中或已完成任务重复执行。

## 0.4.12

- 手动立即追更完成后立即刷新并展开通知中心，不再只显示一次加载动画。
- 没有已播新内容、暂未找到资源、网盘检查失败和内部错误均会生成明确的站内结果通知。
- 已创建转存任务时复用转存结果通知，继续反馈已提交、待确认、成功或失败，避免重复通知。

## 0.4.11

- 通知中心卡片优先展示由 MediaIndex 后端缓存的海报，图片不可用时自动回退为原有状态图标。
- 企业微信自建应用、企业微信群机器人和 Telegram 优先发送带海报的图文通知，发送失败时自动回退为文字消息。
- 智能追更和愿望单进入待确认后，可自动向企业微信发送最多 5 个编号候选，成员回复数字即可继续处理。
- 通知设置新增公网访问地址，用于企业微信回调、通知跳转和缓存海报访问。

## 0.4.10

- 修复手动执行追更会绕过未来发布日期的问题；手动执行现在只跳过当天的追更时刻，绝不会提前搜索未来集。
- QAS 已触发任务改为每 10 秒确认一次，智能追更页同步每 10 秒静默刷新，缩短转存完成后的状态回写等待。

完整发布说明、验证结果和固定镜像标签请查看 [GitHub Releases](https://github.com/xudong7587/media-index/releases)。

## 0.4.9

- 智能追更时间改为显式保存，时间输入、保存按钮与任务操作按钮统一对齐。
- 设置页新增吸顶保存栏，可在基础设置和通知设置任意滚动位置保存当前子页。

## 0.4.8

- 智能追更按任务设置的发布时间执行，到点后先刷新 TMDB 剧集信息并扫描网盘已存进度。
- 仅对 TMDB 明确已播出且网盘未保存的新集搜索资源；缺少播出日期时只刷新元数据。
- 智能追更页面新增每个任务独立的追更时间设置。
- 页面右上角新增 GitHub 仓库入口。

## 0.4.7

- 企业微信资源搜索在结果不明确时返回最多 5 个编号候选，不再自动选择第一项。
- 支持通过编号选择资源、待确认任务和资源候选，选择状态按 UserID 保存 30 分钟。
- `/review` 接入既有安全确认与重新匹配流程。
- 设置页拆分为“基础设置”和“通知设置”，并加入内置指令速查。

## 0.4.6

- 企业微信资源任务完成后优先发送带海报的图文消息。
- 海报先缓存到 MediaIndex，再通过公开只读接口提供；不可用时自动回退文字。
- 移除通知独立页面，仅保留右上角小铃铛通知中心。

## 0.4.5

- 企业微信自建应用支持直接发送资源名创建转存任务。
- 资源名默认保存到网盘，`本地 资源名` 保存到本地。
- 网页端和企业微信共用任务入队、去重和安全校验逻辑。

## 0.4.4

- 推送测试返回企业微信或 Telegram 的具体错误信息。
- 错误信息中的 access token 和 Bot Token 自动脱敏。

## 0.4.3

- 新增企业微信自建应用交互回调，支持 Token、EncodingAESKey 和成员白名单。
- 实现回调验证、签名校验、AES 解密、企业 ID 校验和重复投递去重。
- 新增 `/status`、`/review`、`/tracking`、`/wishlist`、`/notifications` 和 `/help`。

## 0.4.2

- 新增企业微信自建应用推送，可按成员、部门和标签指定接收范围。
- 企业微信 access token 按应用缓存。
- 企业微信、群机器人和 Telegram 统一提供测试发送。

## 0.4.1

- 新增 Telegram Bot 和企业微信群机器人推送。
- 转存完成、任务提交、待确认、暂无资源和失败事件可推送到外部渠道。
- 升级前的历史事件不会集中补发。

## 0.4.0

- 新增持久化站内通知中心，支持未读筛选、逐条已读、全部已读和清空。
- 覆盖转存完成、任务提交、需要确认、暂无资源和处理失败。
- 通知事件使用稳定业务键去重。

## 0.3.2

- 支持四位集号及连续范围，并避免把年份误识别为集号。
- 裸数字集号只有在文件序列和候选标题均提供足够证据时才允许自动执行。
- 修复候选正文制造虚假片名命中的问题。

## 0.3.1

- 收紧电影候选标题匹配和日期合辑验证。
- 弱标题、弱文件名及无有效资源的候选不再制造无效待确认任务。
